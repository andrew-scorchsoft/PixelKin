{
  "name": "pixelkin",
  "version": "1.2.0",
  "private": true,
  "description": "PixelKin — a retro-styled creature-collecting adventure for the web (and, in time, mobile).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:dist": "npm run build && node tools/audio/compress_dist_audio.mjs && node tools/build/strip_sourcemaps.mjs",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "site": "php -S localhost:8000 -t web tools/dev/site-router.php",
    "preview:release": "npm run release && php -S localhost:8000 -t release",
    "release:site": "node tools/build/assemble_release.mjs --site",
    "release:game": "npm run build:dist && node tools/build/assemble_release.mjs --game",
    "release": "npm run build:dist && node tools/build/assemble_release.mjs"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
